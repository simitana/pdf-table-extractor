services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - LOG_LEVEL=INFO
      - MAX_UPLOAD_SIZE_MB=25
